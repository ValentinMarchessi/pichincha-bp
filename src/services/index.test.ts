import { AssetServices } from ".";

describe("Asset Services", () => {
  beforeEach(() => {
    fetch.resetMock;
  });
  describe("getAssets", () => {
    const assets = AssetServices.getAssets();
  });
});
